# Kizuna の用語と境界

プラットフォーム型の店舗運営システム（CMS/CRM/HRM）。単一のグループが運営するプラットフォームが複数の店舗を管理し、各店舗は自身のサイト・従業員・顧客・注文を運営する。

実装規約は [AGENTS.md](AGENTS.md)、設計理由は [ADR 索引](docs/README.md)を参照する。以下の ADR 番号は同索引から辿れる。未実装事項は決定済みの概念と区別して記載する。

## 用語集

### プラットフォームとテナント

**Tenant（テナント）**:
プラットフォーム管理の文脈で管理される契約対象。店舗と 1 対 1 で対応する。独自のドメインとサイトを持つ。
避ける表現・設計: Shop, Organization

**Store（店舗）**:
Tenant と同一の対象を、店舗運営の文脈から自称したもの。コード上は店舗側スコープの命名接頭辞（StoreProfile、store-orders）。
避ける表現・設計: Branch

**Central（プラットフォーム側、旧称）**:
Central は構造概念としては退場済みで、プラットフォーム管理系権限グループ（`PermissionCode.Console.PLATFORM`）の旧称として残るのみ。機能は職位（ロール）に従い、データは店舗（StoreScope）に従う。Platform と Store は同一システムの 2 つのアクセスコンソール（権限境界）であり、独立した 2 つのシステムではない。

### アカウント

**PlatformUser（プラットフォームユーザー）**:
プラットフォーム共通アカウント。本人種別は STAFF / CAST / MEMBER / SERVICE。人のログイン入口は `/platform/login` に統一し、サーバが返す console と本人種別から平台・店舗・キャスト・会員の画面へ振り分ける。授権はロールと本人の店舗集合で表す。店舗コンソール資格は JWT の `storeBridge` と `/platform/me` の `store_bridge` に同源で公開され、店舗 API は授権店舗を検証する。
避ける表現・設計: PlatformAccount、CentralUser / StoreUser による二重アカウント、ロール名だけでの画面・権限判定

**サービスID（本人種別 SERVICE）**:
定期処理・外部連携の実行主体。資格情報を持たない PlatformUser で、対話ログインしない。自作ロールだけを授与でき、ALL_STORES は明示授与を要する。人と同じ actor 参照・授権語彙を使い、専用管理面で扱う。実行追跡・外部サービス鍵・通知は監査／連携基盤の別課題。理由と構造は ADR 0025。
避ける表現・設計: 人の共用アカウント、サービスIDの別表化、既定ロールの授与

**AuthSession（認証セッション）**:
発行済みの 1 枚の JWT が表す認証状態。トークンは発行時点の**資格情報の版**（credential version — 利用者ごとの単調増分。パスワード再設定・自助変更・停止で増える）を運び、現在の版と一致しないトークンは失効している（ADR 0022）。失効には 2 つの粒度がある。**セッション単位失効**（ログアウト）は当該 1 枚のトークンのみを失効させる。**アカウント単位失効**（版の増分）は当該ユーザーの全セッションを一括して即時失効させ、復活は無い — 再開後も再ログインを要する。
避ける表現・設計: Token の裸使用（token は担体、session は概念）、時刻境界（iat 比較）で失効を判定すること

**緊急昇格（EmergencyElevation）**:
HQ が理由とパスワード再照合を伴って、一店舗の店舗操作権限を一時取得する発動記録。EMERGENCY_ELEVATE を要し、有効期間は一律 60 分。延長は新しい発動で表す。撤回は発動の終了と資格情報の版の増分で行い、発動者の通常セッションも失効させる。変更前後の汎用監査・利用通知は別課題であり、この記録だけでは充足しない（ADR 0024）。
避ける表現・設計: 時限ロール授与、既存行の期限延長、店舗書き込みへの elevation_id 配線

**集合作用域（StoreScope / storeSetFilter / @StoreSetScoped）**:
PlatformUser の授権を表す店舗集合（ALL_STORES または SPECIFIC_STORES の店舗 ID 集合）。読みは Hibernate の第二 filter（`storeSetFilter`）が機構的に濾過する fail-closed 設計（解決不能なら例外）。書きは明示的単一 storeId を受け取り、その storeId が授権集合に含まれるか検証したうえで既存の単店機構（StoreContext + storeFilter）へ委譲する。
避ける表現・設計: 読み・書きを同一機構と混同すること（読みは集合フィルタ、書きは単一 storeId 検証で別経路）

スタッフ管理の行使面は四面に分かれる（#762）— **アカウント管理**（平台コンソール。本人種別 STAFF の全アカウントの停止・再開・パスワード代理再設定を担い、授与は一切扱わない）、**管理者管理**（平台コンソール。HQ 側ロール保持者のアカウントのみを扱う）、**店長設定**（店舗管理内の一節。店長の任命・解任・降格。一店 0..n 人）、**店舗スタッフ管理**（店舗コンソール。店舗側ロールのみのアカウントを扱い、行使者は店長）。**HQ の通常セッションは店舗コンソールに入らない**（ADR 0021。緊急昇格は ADR 0024 の別セッション）— 既定授与に店舗コンソール資格になる権限が無く storeBridge は false で、店舗の面は店舗側ロール保持者だけのものである。対象は HQ_ADMIN/STORE_MANAGER/STORE_STAFF のみで、CAST/MEMBER は別チケットの専用フローが扱う。停止（enabled=false）とパスワード変更・再設定は資格情報の版を増やして即時失効する（ADR 0022）。ロール・店舗集合の変更は従来どおり次回ログイン反映。

**Role（ロール）/ Permission（権限）**:
Permission は機能権限の目録で、PermissionCode から播種する。Role はその束で、既定ロールは改廃不可、自作ロールは管理可能。店舗集合は Role ではなく PlatformUser に属す。authority は PERM_ 接頭辞の権限で、ROLE_CAST / ROLE_MEMBER は本人種別の標識。

管理は Owner 層（ROLE_MANAGE: ロール定義・管理者管理・店長任命）、委譲層（STORE_STAFF_MANAGE: 店舗スタッフ）、防提権の守衛に分かれる。店舗側ロールは STORE / SHARED 権限のみで構成し、PLATFORM を含むものは HQ 側となる。委譲先は本人の店舗部分集合・店舗側アカウントに限定し、再委譲権限を持つロールは付与不可。Owner は属性ではなく、有効な ROLE_MANAGE 保持者をゼロにしない守衛で表す。詳細は ADR 0020 / 0021。
避ける表現・設計: Capability、ロールへの店舗紐付け、店長による店長任命、is_owner

**店舗コンテキスト（Store Context）**:
現在店舗・授権店舗・切替・店舗リンクの共通境界。StoreContextProvider は JWT の storeBridge を読み、資格があるときだけ店舗一覧を取得する。本人資料は別の MeProvider が共有する。店舗入口 `/store/entry` は前回選択とメニューから到達可能な着地先を解決し、取得失敗・到達先なしはその場で示す。実装上の使い分けは [frontend/AGENTS.md](frontend/AGENTS.md)。
避ける表現・設計: 消費者ごとの me()/stores() 再取得、店舗パスの手組み、固定ページへの着地

### 店舗運営

**Cast**:
店舗を跨いで同一人物を表すプラットフォーム級集約。CAST 種別の PlatformUser と 1:1 で、本人同一性はアカウント同一性に一致する。招待受諾で作成・解決し、本名と生年月日（いずれも任意）を持つ。本人照会は平台専属で読み取り専用。本人・在籍・公開プロフィールの三層は報酬項目を持たない（ADR 0026）。
避ける表現・設計: Cast を店舗在籍の意味で使うこと、別アカウント間の自動人物統合

**CastEnrollment（店舗在籍）**:
一人の一店舗での在籍区間。ENROLLED ⇄ SUSPENDED、両者 → WITHDRAWN と遷移し、退店は終端、再入店は新行。有効在籍は一人一店最大一件。招待受諾前の cast_id は空でよく、店舗の先行作成が可能。本人紐付けは招待受諾だけで行い、同店重複は 409 として招待を消費せず巻き戻す。

招待は在籍あたり有効一枚で、再発行は旧 PENDING を失効させる。紐付け済み・退店済みへの発行を拒否し、受諾時も退店を再検する。退店でアカウントの店舗集合は自動変更せず、業務可否は在籍条件で制御する。注文・申請・シフト・実績・招待の店舗側 cast_id はこの在籍 ID を指す。内部 custom fields を保持し、状態操作の履歴と内部値の編集前スナップショットを記録する（ADR 0026）。
避ける表現・設計: ACTIVE/INACTIVE、退店行の復活、店舗作用域から本人 Cast への直接参照

**CastProfile（公開プロフィール）**:
在籍と 1:1 の公開専用集約。源氏名・写真・紹介文・展示用年齢／身体情報・表示順・公開 custom fields の正本であり、内部画面も同じ源氏名を読む。展示用年齢は本人の生年月日と別概念。再入店時は新規作成し、旧値を自動継承しない。

公開状態は PUBLISHED / UNPUBLISHED（作成時 UNPUBLISHED）。露出条件は PUBLISHED ∧ ENROLLED。公開 DTO は在籍状態・監査日時・本人情報・内部値を型に持たない。custom field 定義の公開区分は作成時固定で、削除・再作成による値の復活も認めない。公開切替に専用履歴はない。詳細は ADR 0026。
避ける表現・設計: 内部情報を実行時フィルタだけで隠すこと、公開区分変更による値の移送

**Customer（顧客）**:
店舗の顧客。CRM の管理対象。単一の店舗に帰属する。電話番号は店舗台帳内の検索の**手がかり**であって同定の証明ではない — 受注録入の照合キーとしては使うが、複数一致時は自動照合せず（顧客未設定で成立させる）、会員への帰属根拠には一切しない（ADR 0008 / 0009）。照合キーは第一電話番号のみで、第二番号は連絡先情報に留まる。
避ける表現・設計: 電話番号を一意キーや本人証明として扱うこと

**顧客統合（Customer Merge）**:
同一店舗の重複 Customer を、人が確認して統合する。注文と関連区間を存続行へ付け替え、旧行は統合先を指す墓標として残す。連鎖統合でも旧 ID は一跳で存続行へ届く。別会員の ACTIVE 関連が両行にあれば拒否し、先に解除する。フィールドの自動合併・undo はなく、統合履歴を手動修復の根拠とする。台帳・受注帰属には波及しない（ADR 0010）。
避ける表現・設計: 行の削除、自動統合、値の静かな合併

**Member（会員）**:
プラットフォーム級の会員身分。店舗 CRM の Customer とは別集約で、複数店舗の顧客台帳と関連を持てる。会員でない Customer も成立する。実装の正本は [Member](backend/src/main/java/com/kizuna/member/domain/Member.java)。
避ける表現・設計: Member と Customer の混用、会員を店舗台帳の意味で使うこと

**関連（Customer–Member Link）**:
会員（Member）と店舗顧客台帳の行（Customer）の紐づけ。1 行が 1 区間（ACTIVE → RELEASED）を表す追加型の記録で、**成立根拠**（MEMBER_CODE＝会員コード提示 / MEMBER_REQUEST＝会員申請の確定時自動整備 / MIGRATION＝旧システム移行由来）を事実として持つ。根拠は行ごとに不変で挙動を分けない（MIGRATION も全機能）。より強い根拠での再成立は解除→新しい行で表す。解除は**以後**の帰属を止めるだけで、過去に確定した受注帰属記録・ポイントには波及しない（ADR 0009）。店舗ごとに会員 1 人につき高々 1 本、顧客 1 行につき高々 1 本の ACTIVE。
避ける表現・設計: 電話番号一致を成立根拠にすること、FHIR 式の抽象等級（level1〜4）で根拠を表すこと、解除で過去の履歴を消すこと

**PointLedger（会員ポイント台帳）**:
プラットフォーム層にある会員ポイントの唯一の正本。全増減（付与・利用・取消・利用取消・失効・手動調整・退会消去）を仕訳（種別・理由・実行主体・元取引・発生店舗・日時）として記帳し、残高は仕訳から再構成する。期限別残高を持ち、利用は期限の早い仕訳から充当する。通常付与（受注完了に伴う付与）のポイントは無期限で、期限を持つのは特典規則・手動指定による付与だけ。期限切れは残高から除外され、日次で失効仕訳として顕在化する。会員（Member）に帰属して店舗を跨ぐ（A 店獲得→B 店利用）。発生店舗は帰属属性 `originating_store_id` であり StoreScope の隔離軸ではない（ADR 0006）。非会員（未紐づけ顧客）にポイントは存在しない。
避ける表現・設計: 店舗レベルの残高列（旧 Customer.points — 廃止済み）を復活させること、残高だけを保存して仕訳を省くこと

**会員ランク（Member Rank）**:
会員（Member）に帰属するプラットフォーム級の等級。BRONZE → SILVER → GOLD の三値（初期値 BRONZE）で、跨店舗の累計実績 — 会員へ帰属した完了受注の回数と、受注付与（ORDER_GRANT）の累計純額（取消仕訳控除後 — 指標自体は減り得る）— のいずれかが閾値（SystemConfig）に達したとき昇格する。判定は**帰属の成立**と同期する（受注完了時、および伝票トークンによる事後申領時 — 付与の有無に依らず判じるので、会計 0 円や付与単位に満たない来店も回数へ算入される。バッチは無い）。**棘輪（ラチェット）** — 降格は存在せず、昇格後に根拠となった付与が取消仕訳で控除され指標が閾値を割ってもランクは戻らない。手動変更の口は無く、現在値と更新履歴（いつ・何から何へ・契機の帰属記録、付与を伴うならその仕訳）を保持する。ランクは付与率に影響しない — 付与規則は「基準単位×基準ポイント」一本（#381 要件 4 の残項は不採用で確定）。
避ける表現・設計: 店舗級の Customer.rank（撤去済み — 顧客台帳にランクの列も欄も無い）、ランク別付与率の再導入、手動でのランク上書き

**特典規則（Benefit Rule）**:
紹介・ログイン・来店を契機とする追加ポイント付与の規則。プラットフォームが管理する独立の集約で、五要素 — 適用組織/店舗・条件・期間・重複可否・取消方法 — を明示して定義する。適用組織/店舗は**発火側**の絞り込み（どの店舗での事象が条件を満たすか — 全店舗または店舗集合）であって、付与されたポイントの利用範囲ではない（ポイントは常にプラットフォーム級 — ADR 0006）。条件は閉じた種別 — 紹介（被紹介者の初回有料受注完了で紹介者・被紹介者の双方へ付与）・ログイン・来店（会員へ帰属した受注の完了）— で表し、自由な条件式は持たない。発火事象が店舗文脈を持たないログイン規則は全店舗のみを取る。「期限付き施策」は独立の種別ではなく、任意の規則が**適用期間**を持つことで表す。期間は二つの別概念からなる — 規則の適用期間（発火の窓）と、付与ポイントの有効期間（付与日から起算する期間で、記帳時に期限日へ固定される。この規則と手動指定だけが期限付きポイントの産地 — PointLedger の項）。重複可否は「同一会員が同じ規則で**別の**発火事象により再び受益できるか」の規則側属性 — 同一の発火事象を二度記帳することは重複可否に依らず不正。紹介の一回性は条件側（被紹介者の初回受注）にあり、紹介規則は被紹介者ごとに独立の事象として毎回を取る（紹介者は紹介した人数ぶん受益できる）。取消方法は種別から導く — 受注を条件とする付与は根拠受注に連動して取り消され、ログイン付与に取消は無い。受注条件の判定はランクと同じく帰属が物化する契機（受注完了・伝票トークンの事後申領）と同期する。付与量は規則ごとに定める固定点数（紹介は紹介者・被紹介者の二値）で、受注付与の計算規則（基準単位×基準ポイント）へ倍率を掛ける形の施策は持たない。規則は停用で退場し、削除しない。
避ける表現・設計: 倍率型（付与率 × N）の施策、期限付き施策を独立の種別にすること、適用店舗をポイントの利用範囲と読むこと、規則の物理削除

**ポイント巻き戻し（Point Rewind）**:
受注 1 件を根拠とするポイントの影響を台帳側で打ち消す明示操作。当該受注の全付与（受注付与、および受注を条件とする特典付与）の未消費残を取消仕訳で無効化し、当該受注の利用を**利用取消**仕訳で逆転する。利用取消は元の利用仕訳を参照し、引き当てを逆転して**元のロットへ残量を戻す** — 期限は元のまま（期限付きポイントが無期限へ洗い替わらない）— で、自身は新しいロットにならない。1 件の利用への利用取消は高々 1 回。操作者が理由付きで起こし、**宛先に取れるのは完了した受注だけ**（確定済み・取消済みには打ち消す授受が構造上無く、確定済みを許すと後の完了が積む付与を打ち消せなくなる） — 完了後訂正の門は台帳へ依存を持たないまま（零自動波及 — ADR 0019 不変）で、門の画面はこの操作への導線を示すに留まる。消費し切った付与の取戻しと差額の手当ては別段の手動調整・帰属訂正の領分。巻き戻し済みの受注は伝票トークンの事後申領を受け付けない — 申領が新しい付与を積み直さない。
避ける表現・設計: 完了後訂正の門から自動で引くこと、利用の逆転を新規ロットの再付与で表すこと、受注取消（CONFIRMED→CANCELLED）と混同すること

**Order（注文）**:
顧客の店舗における 1 回の予約／受注記録。Customer、キャスト（参照先は店舗在籍 = CastEnrollment）、および接客担当（PlatformUser）に紐づく。店舗への帰属は `store_id`（StoreScopedEntity）のみで表し、店舗名を非正規化して保存しない（表示が必要な場合は Store への JOIN で導出する。ADR 0003）。
**すべての受注は確定で出生する**（ADR 0017）。店舗が起こす受注は電話口で受けると決めた時点で可否が判断済みであり、会員ポータルと公開店面からの申請は別記録の**予約申請（OrderApplication）**が受けて、店舗の確定操作が CONFIRMED の受注を生む。状態機械は CONFIRMED → COMPLETED / CANCELLED のみ。**受付経路 MEMBER_WEB / GUEST_WEB は申請確定由来の受注だけが名乗る**値で、会員ポータル由来が MEMBER_WEB、公開店面のゲスト申請由来が GUEST_WEB になる。どちらも店舗の作成経路では拒否される（広告費と効果集計の根拠になる記録が代理入力で偽装されないため）。
受注の「人」への帰属は Customer（店舗台帳行）に着地する。会員の申請による受注も確定時に台帳行と関連が整えられ、完了時の会員解決（ポイント記帳先）は「顧客 → 有効な関連 → 会員」の一本だけを通る（ADR 0008）。申請者の記録（requester）は申請の出所であって帰属ではない。顧客にも会員にも帰属しない受注（**無帰属受注**）は正規の状態として成立し、売上には数えるがポイントは記帳されず、店舗のみが見る。
**取消**は確定した受注を終わらせる唯一の操作で、理由・実行者・時刻を伴い、二度は行えない。理由は「なぜこの受注が消えたか」を後から辿るための人の判断の記録であって、集計のための分類軸ではない。**取消済み・完了済みは終端**であり、内容の書き換えも行の削除も受け付けない — 誤登録も行を消さず取消として残す。完了後に内容を直すことはこれとは別の概念で、権限と訂正の記録を伴う専用の経路（**完了後訂正**）が担う（ADR 0019。財務ロックを設けないという方針は変えない）。
避ける表現・設計: Reservation, Booking、申請の出所（requester）を帰属として扱うこと、受注行を削除して無かったことにすること、取消理由を集計の分類軸として使うこと、受注を未確定で生むこと

**受注明細（Order Fee Line）**:
受注金額の内訳 1 行（`t_order_fee_lines`）。**種別＋名称の写し＋帯符号の金額**の三つを持ち、行に同一性は無い（書き込みは常に差し替えで、行を名指して直す口は無い）。種別は**八枚の閉集合** — BASE_COURSE / EXTENSION / OPTION / SURCHARGE / DISCOUNT / MANUAL_ADJUST / POINT_REDEMPTION / CREDIT_SURCHARGE。指名・受付区分・場所エリア等の加算は SURCHARGE へ集約し、区別は名称の写しが担う。**ホテル代・交通費・釣銭は受注金額外の回収・精算項目**であり、種別を持たないことで型から排除される（横断形式は #386）。
**符号は種別が決める** — 加算の種別は 0 以上、減算の種別（割引・ポイント利用）は 0 以下、手動調整だけが両方を取る。行レベルの DB CHECK が同じ規則を持つ。表示層（DTO・UI）は**減項を正値へ翻して見せ**、入力にも符号を書かせない。
**合計（`total_fee`）は導出**で、手入力の口は無い。行を動かす集約の行為メソッドが毎回総和を書き直し、「内訳の和＝合計」は構造で成立する（集約経由では和≠合計を作れない。ADR 0018）。合計を機械和から外したい場面は**手動調整の行**で表す。ポイント利用も減算の行なので、合計はポイント控除後の請求額にあたる — 付与の基準と利用の上限はその行が入る前の総和で決める。**ポイント利用の行はシステム専有**で、完了処理が台帳の減算仕訳と対で書き、店舗の通常編集では作れも消せもしない — **完了後訂正の門内でも編集できず**、利用の誤りはポイント機構（手動調整・帰属の訂正）経由で直す。他の七種は店舗の手入力で、本段階では自動算出を一切持たない（算出規則の正本は #384）。
**行は負を取れるが、総和は負にならない** — 内訳の総和が負になる書き込みは経路を問わず拒む。過剰割引・返戻は受注金額の内側に表現を持たず、回収・精算（#386）の領分になる。**完了は取得時の受注の版を名指して送る** — 会計の場を開いている間に別の操作者が受注を動かしていれば、書かずに差し戻される（ADR 0018）。終端前の通常編集は後書き勝ちのまま — 編集同士の上書きは再編集で直せるが、完了は終端化で救済が訂正の門しか無いため、入口にだけ照合を課す。
避ける表現・設計: 合計を手入力して内訳と別に持つこと、加算項目ごとに `t_orders` へ列を足すこと、減項を正値で保存すること、ポイント利用の行を店舗の編集で触らせること、受注金額外の回収・精算項目を種別として足すこと、総和が負になる内訳を保存すること、完了要求を版の照合無しで受け付けること

**コーススナップショット（Course Snapshot）**:
受注が持つ `course_name` / `course_minutes` / `extension_minutes`。語義は「**本受注に実際に適用された内容の写し**」であり、確定の瞬間を凍結した帧ではない — 写入時点は店舗の確定＝受注の出生で、サービス中の変更（延長・コース変更）は終端に入るまで店舗が更新できる。終端後の変更は完了後訂正の門だけが担う（ADR 0019）。**会計が快照の最後の更新機会**なので、完了要求そのものでも course_name を直せる。基本コース料金（BASE_COURSE）の行の名称はこの写しから採り、行の側に別の名前を名乗らせない。
**マスタ側からの回写は永久に禁止**する（ADR 0018）。サービス定義の正本（#384）が建った後も列は動かず、替わるのは充填元（手入力 → 引用）と追補される引用/版本列だけで、マスタの後続変更が既存の受注へ遡ることはない。
避ける表現・設計: マスタ変更を既存受注の写しへ反映すること、写しを確定時の凍結帧として扱い終端前の更新を塞ぐこと、写しを JSON 一列にまとめること

**完了後訂正（Order Correction）**:
完了した受注（Order）の内容を権限付きで直す唯一の口（ADR 0019）。**状態は動かさない** — COMPLETED → CONFIRMED の回退は開けず、CANCELLED は門の対象外（誤取消の救済は同内容で受注を起こし直すこと。取消 ADR 0013 が守った初回理由・実行者の保護を訂正口で迂回させない）。直せるのは**三組だけ** — 受注明細行（POINT_REDEMPTION を除く七種。合計は集約が再導出）・**実績時刻**（actual_arrival_time / actual_end_time。門が唯一の書き込み口）・コーススナップショット（金額行と対で直せる。金額だけ直る半修状態を作らない）。それ以外（予定時刻・人数・指名・受付担当・備考・伝言）は ADR 0013 の凍結のままで、凍結は要求の型に項目を持たせないことで表す。
権限は **ORDER_CORRECT**（店長限定）。日常権限の ORDER_MANAGE で守ると「権限のある利用者のみ」が空文になる。入口は専用端点で**理由必須**（凍結済みの記録を動かす特権操作。分類軸ではない）、要求は三組の**全量**を運ぶ（省略は「変更しない」ではなく「値なし」— 実終了時刻・延長分数を空へ戻す訂正が要るため）。
痕は**就地更新＋訂正前快照行**（`t_order_corrections`。1 回の訂正＝1 行。当日実績の訂正履歴の拡張）。標量前値は列で、行に同一性の無い明細の前値は行集合全体の jsonb 写しで持つ。ある訂正の「後値」＝次の訂正の「前値」または本体の現値なので、**前値の鎖で履歴を復元できる**。受注側の痕はここまでで、訂正仕訳の横断形式は精算の領分。
**ポイントへは零自動波及**。完了時の自動付与は「完了時点の合計に基づく時点事実」（帰属 ADR 0009 と同族）で、訂正で合計が変わっても追随しない。門は台帳へ**依存を持たず、付与の差額は算出も提示もしない**（理由は ADR 0019）。画面が名乗るのは「付与は動かない」ことと、どの会員の台帳を見ればよいか（帰属記録が持つ会員コード）まで。
避ける表現・設計: 訂正を状態の回退や取消の代用にすること、ポイント利用の行を門から触らせること、付与を訂正へ自動追随させること、凍結字段の拒否をサービス層の項目リストで表すこと、手当てと結ぶ線が無いまま付与差額を可執行の指示として出すこと

**予約申請（OrderApplication）**:
会員または公開店面のゲストが提出する来店希望で、Order とは別の記録。PENDING → CONFIRMED / DECLINED / WITHDRAWN。確定は補完済み内容から CONFIRMED の Order を作成して order_id を申請へ記録し、申請原文は保存する。謝絶は理由必須。希望営業日を過ぎた PENDING は失効表示を導出し、確定・謝絶を拒否する（本人の取り下げは可能）。

会員申請の名乗りは顧客作成に使い、平台プロフィールの表示名・メールは店舗に渡さない。顧客解決は確定時の関連に基づく。ゲスト申請は公開入口の検証とレート制限を受ける。ADR 0017 と [申請入口](backend/src/main/java/com/kizuna/order/api/store/OrderApplicationController.java)を参照。
避ける表現・設計: Order の CREATED 状態、申請原文の上書き、失効バッチ、集約名 Request

**受注帰属記録（Order Attribution）**:
受注 1 件が会員へ帰属した事実の受注単位の記録（根拠・帰属時刻・帰属時点の会員コードのスナップショットを持ち、会員の削除後も残る）。帰属は**完了の瞬間**（完了時の関連解決 — 根拠 COMPLETION）または**事後申領の瞬間**（伝票トークン — 根拠 RECEIPT_TOKEN）にだけ生まれ、成立後は関連の状態に影響されない**不変の事実**である（ADR 0009）。会員の来店履歴はこの記録から読む（関連成立以前の受注は帰属記録が無いので見えない）。事後申領の証明は、完了時に会員へ帰属しなかった受注（顧客の有無を問わない）へ発行されるワンタイムの伝票トークン（QR）の所持のみで、申領期限は完了から 90 日（訂正による再発行のみ例外で、再発行から 90 日）。事後申領は店舗台帳（Customer 行・行級の関連）に一切波及しない。関連の成立が過去受注の遡及帰属を引き起こすことはない（ADR 0008）。唯一の訂正操作は理由付きの**無効化**で、ポイントへは自動波及しない（台帳側の訂正は帰属記録を宛先に取る別段の手動調整で行う。ADR 0012）。無効化された受注は会員へ帰属しない状態に戻り、トークン再発行による事後申領の対象へ復帰する。生きた伝票は受注 1 件につき高々 1 本で、再発行は前の 1 本を**失効**させる（機構が起こす副作用であり、人手の無効化とは別の操作。発行の応答を取り逃した店舗は再発行し直せる。ADR 0011）。
**帰属の物化**は、帰属記録の確定と、その契機に適用されるポイント利用・付与、来店特典、会員ランクの判定を一体で成立させること。付与が 0 ポイントでも帰属は成立し、来店回数に数えられる。
避ける表現・設計: 注文番号の入力を所持の証明として受け付けること、行級の関連（リンク）との混用、無効化を取消連動（受注取消）の代用にすること

**指名 / 指名候補（Nomination / Cast Candidate）**:
Order に紐づく 1 名のキャストが**指名**（参照先は店舗在籍 = CastEnrollment）。**指名候補**は「当該店舗の在籍中（ENROLLED）の在籍」であり、この条件は候補の読み口と書き込み側の検証がひとつの述語として共有する — 候補に出さないだけでは、在籍 ID を直接送る要求を防げない。確定（Order の確定操作）ではさらに当日の確定シフトを要求する。
会員の指名候補は、さらに公開中（PUBLISHED）のプロフィールと公開可の確定シフトを持つ在籍に限る。会員の指名申請にも同じ公開境界を適用する。店舗内部の指名資格はプロフィールの公開状態に依存しない。
店舗側の候補の読み口は受注の配下（受注権限）に置く。指名は受注の操作であり、候補の範囲も要る権限も受注側が決めるため。キャスト管理の一覧は別物で、在籍停止も返し、キャスト管理権限を要求する。
避ける表現・設計: キャスト管理一覧を候補として流用すること、絞り込みを画面側だけで行うこと

**出勤希望（ShiftRequest）**:
キャストが所属店舗を指定して提出する申請（店舗・日付・時間帯・備考）。種別は 2 つ — **新規希望**（NEW: 承認で確定シフトを新規作成）と、確定済みシフトに対する**変更申請**（CHANGE: 承認で対象シフトを更新）。状態は受付済み（PENDING）・承認（APPROVED）・却下（DECLINED）の 3 つ。承認しても希望そのものはシフトへ変化せず、申請の履歴として残る — NEW の承認時は生成したシフトの id を申請行（shift_id）へ回写し、系列（下記 Shift）の背骨とする。所属店舗の判定は「当該店舗に本人の未退店（非 WITHDRAWN）の在籍（CastEnrollment）が存在すること」で、在籍停止（SUSPENDED）でも提出できる（アカウント停止と在籍停止は別概念であり、退店で所属が切れる）。この所属判定は提出時と承認時の両方で課す — 提出後に退店した本人の PENDING の希望を、承認がシフトへ物化させない。これは出勤希望の所属判定に限った話であり、在籍状態を可否判定に使わないという通則ではない — 受注の指名可否（上記「指名 / 指名候補」）は ENROLLED を要求する。未取消の当日実績が付いたシフトへの CHANGE は受け付けない（提出時拒否・承認時再検・承認可否導出の三面同条件）。目標営業日が終了した申請は承認できない（判定は日付変更時刻。自動失効は無く、却下は無期限に可能）。
避ける表現・設計: 希望とシフト（Shift）の混用、変更申請を独立した集約として扱うこと

**Shift（シフト）**:
排班の事実の唯一の正本（`t_shifts`）。状態は **TENTATIVE（店舗内の下書き）** と **CONFIRMED（確定）** の 2 値で、CONFIRMED → TENTATIVE の逆行も許す。TENTATIVE の負向不変量は 3 条 — 店外へ公開されない・受注確定の内部検証（hasConfirmedShift）に数えない・変更申請の対象にならない。系列は**双起点一主幹**: キャスト希望起点（申請 → 承認 → シフト。申請行の shift_id 回写が背骨）と店舗直接作成起点（申請行なし）のどちらも正規の起点で、t_shifts の主幹に合流する。店舗の直接編集は「承認後の調整」として維持する。ただし未取消の当日実績が付いたシフトは work_date とキャストを変更できず（実績が物化した事実と食い違う付け替えを許さない — ADR 0014）、実績行（取消済みを含む）が参照する限り削除できない（誤建の組は実績の取消標記とシフトの TENTATIVE 化で中性化する）。
避ける表現・設計: 出勤希望（ShiftRequest）との混用、TENTATIVE を「未公開の確定シフト」と読むこと

**シフトの公開可否（Shift Publication）**:
「店外（匿名訪問者+会員）へこのシフトを露出してよいか」の行単位フラグ（既定 = 承認時公開可。非公開化が例外操作で、内密の出勤は承認・作成と同一トランザクションで非公開を指定して出生させる — 公開で生まれてから隠すまでの露出窓を作らない）。シフト側の露出関門は **CONFIRMED ∧ 公開可**で、公式サイト出勤表と会員ポータルの指名候補の両方がこの述語で絞る（キャスト在籍などシフト外の既存条件はそのまま重なる — 単独の十分条件ではない）。片面だけ絞ると軸が形骸化し、開放登録の会員経由の列挙経路になる。同じ理由で、会員経由の指名の**書き込み検証**も候補の読み口とこの述語を共有する — 候補から隠すだけでは cast_id の直送を防げない（上記「指名 / 指名候補」の共有述語規則）。承認とは独立の軸であり、変更申請の承認でシフトが更新されても公開可否は保持される。**負向不変量**: 受注確定の内部検証（hasConfirmedShift＝店舗の確定操作）と店舗側管理画面は公開可否を見ない — 公開可否は店外露出のフィルタであって状態機械の一部ではない（ADR 0015）。切替は SHIFT_MANAGE の日常業務で、専用の留痕は持たない。
避ける表現・設計: 公開可否を承認状態と混同すること、店舗内の可視制御に流用すること

**当日実績（Attendance）**:
キャストが実際に出勤した事実の一等記録。シフト（予定）とは別集約で、キャスト・店舗・営業日・実際の開始/終了時刻（暦日付き — 事後補記でも帰属営業日が一意に決まる）を持ち、確定シフトへの可空参照（shift_id。1 シフトにつき**未取消の**実績は最大 1 行）を張る（ADR 0014）。予定なしの**飛び込み出勤**は shift_id = NULL の一等記録。行の存在そのものが出勤確認であり、確認フラグ・状態機械は持たない — 欠勤は「その営業日に確定シフトがあり、未取消の実績が無いこと」から（キャスト・店舗・営業日）粒度で導出する（判定は営業日の終了とシフトの予定終了の両方を待つ。未来・進行中は欠勤に数えない）。実績は受注確定の根拠にならない（飛び込みで指名を受けるには従来通り店舗が確定シフトを直接作成する）。記録は SHIFT_MANAGE 保持者（店舗側）が行い、キャスト本人の打刻は無く、事後補記の時限も設けない。**訂正**は就地更新と同時に訂正履歴（編集前の行スナップショット・訂正者・時刻）を残す。実績は物理削除しない（保存理由は ADR 0014）— 誤建行は取消標記とし、導出・照会から除外する。待機場所は実績行の可空自由記述列。
避ける表現・設計: 実績をシフトの状態遷移（scheduled → worked）で表すこと、実績行の物理削除

**営業日（Business Date）**:
暦日から独立した帰属単位。プラットフォーム統一の日付変更時刻（SystemConfig）で区切り、帰属は分割しない（全量帰属。25 時閉店の店の深夜勤務は前営業日に属す）。実績行に business_date として物化する。帰属の決め方は二本で、優先順がある — **シフトに紐づく実績はシフトの work_date を継承**（予実は常に同一営業日。実際開始が日付変更時刻を跨いでも変わらない）、**飛び込み（shift_id なし）は実際開始時刻の属する営業日**へシステムが自動判定する（誤りは訂正で直す）。「営業日が終了した」の判定（期限切れ申請の承認拒否・欠勤導出など）もこの日付変更時刻で行い、暦日零時を使わない。日付変更時刻の変更は自由で、将来の判定にのみ作用する（不遡及 — 物化済みの営業日は書き換えない）。
避ける表現・設計: 暦日 0 時での帰属・期限判定、店舗別の日付変更時刻（実需が確認されるまで）

**StoreProfile（店舗サイト設定）**:
店舗サイトのブランドと外観の設定（テンプレート、logo、banner、SNS リンク等）。
避ける表現・設計: TenantConfig（旧名。SystemConfig と紛らわしい）

### プラットフォーム設定

**SystemConfig（システム設定）**:
プラットフォームレベルのキーバリュー設定（SMTP、メンテナンスモード等）。SYSTEM_CONFIG_MANAGE 権限の保持者のみが管理できる。
避ける表現・設計: Config（裸使用。StoreProfile と混同しやすい）

**Menu（メニュー）**:
管理画面のナビゲーションメニューツリー。Platform と Store の 2 スコープで同一概念を共有する。コード上は単一の Menu 集約（`t_menus`）へ統合済み（2026-07-18、#404 決定 2 / #409。CentralMenu / StoreMenu は統合前の歴史名）。
避ける表現・設計: TenantMenu（旧名）

## 未解決の論点

- **Customer.classification**：自由テキストで、区分の正式な値体系は未定義。UI はテキスト入力で暫定対応し、業務側で値集合が確定次第 enum + ドロップダウンに収束させる。（rank は会員ランク（Member 帰属・enum）へ移り、店舗級の Customer.rank は撤去済み。「会員ランク」の項を参照。）
