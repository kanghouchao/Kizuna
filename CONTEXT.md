# Kizuna

プラットフォーム型の店舗運営システム（CMS/CRM/HRM）。単一のグループが運営するプラットフォームが複数の店舗を管理し、各店舗は自身のサイト・従業員・顧客・注文を運営する。

## Language（用語集）

### プラットフォームとテナント

**Tenant（テナント）**:
プラットフォーム管理の文脈で管理される契約対象。店舗と 1 対 1 で対応する。独自のドメインとサイトを持つ。
_Avoid_: Shop, Organization

**Store（店舗）**:
Tenant と同一の対象を、店舗運営の文脈から自称したもの。コード上は店舗側スコープの命名接頭辞（StoreUser、store-orders）。
_Avoid_: Branch

**Central（プラットフォーム側、旧称）**:
Central は構造概念としては退場済みで、プラットフォーム管理系権限グループ（`PermissionCode.Console.PLATFORM`）の旧称として残るのみ。機能は職位（ロール）に従い、データは店舗（StoreScope）に従う。Platform と Store は同一システムの 2 つのアクセスコンソール（権限境界）であり、独立した 2 つのシステムではない。

### アカウント

**PlatformUser（プラットフォームユーザー）**:
プラットフォーム共通アカウントとしての「プラットフォーム身分」。email でログインし、授権は「ロール×店舗集合」（店舗集合は「全店舗」「個別店舗」の 2 種のみ）で表す。旧 CentralUser / StoreUser の二本立て認証は撤去済みで、PlatformUser が唯一のアカウント種別である（#326）。
_Avoid_: PlatformAccount、「テナントユーザー」系の呼称

統一ログイン（`/platform/login`）はロールに応じて自動ルーティングする（HQ_ADMIN → Central、STORE_MANAGER/STORE_STAFF → Store）。店舗コンソールは平台トークン + `X-Store-ID` を集合作用域（授権店舗集合）で fail-closed 検証したうえで旧業務 API に過橋する（#324）。この過橋機構は撤去せず恒久的に運用する（旧 CentralUser/StoreUser の二本立て認証自体は #326 で撤去済み）。過橋資格（店舗コンソール権限の保持）はログイン時に JWT の `storeBridge` claim として確立され、`GET /platform/me` の `store_bridge` にも同源で露出される（フロントエンドに権限→コンソールの対応表を複製させない — #428）。

**AuthSession（認証セッション）**:
発行済みの 1 枚の JWT が表す認証状態。失効には 2 つの粒度がある。**セッション単位失効**（ログアウト・パスワード変更 = token ブラックリスト）は当該 1 枚のトークンのみを失効させる。**アカウント単位失効**（スタッフ停止 = user ブラックリスト）は当該ユーザーが保有する全セッションを一括して即時失効させ、再開すれば即時に解除される。
_Avoid_: Token の裸使用（token は担体、session は概念）

**集合作用域（StoreScope / storeSetFilter / @StoreSetScoped）**:
PlatformUser の授権を表す店舗集合（ALL_STORES または SPECIFIC_STORES の店舗 ID 集合）。読みは Hibernate の第二 filter（`storeSetFilter`）が機構的に濾過する fail-closed 設計（解決不能なら例外）。書きは明示的単一 storeId を受け取り、その storeId が授権集合に含まれるか検証したうえで既存の単店機構（StoreContext + storeFilter）へ委譲する。
_Avoid_: 読み・書きを同一機構と混同すること（読みは集合フィルタ、書きは単一 storeId 検証で別経路）

スタッフ管理（`/platform/staff`、HQ_ADMIN 限定）で PlatformUser のロール×店舗集合を付与・変更できる（#325）。対象は HQ_ADMIN/STORE_MANAGER/STORE_STAFF のみで、CAST/MEMBER は別チケットの専用フローが扱う。停止（enabled=false）だけが唯一の例外で即時失効する。ロール・店舗集合の変更は従来どおり次回ログイン反映。

**Role（ロール）/ Permission（権限）**:
授権モデルは伝統的な RBAC。**Permission** は機能権限の目録行で、コード定義（`PermissionCode` enum）を `t_permissions` へ播種したものが正本であり書き込み API を持たない。**Role** は権限の束（`t_roles` + `t_role_permissions`）で、平台既定ロール（`is_system=true`、播種の 3 件）は改廃を拒否し、利用者は `/platform/roles` から自作ロールを自由に追加・改廃できる。**ロールは店舗を持たない** — 担当店舗集合は PlatformUser 側（StoreScope）に付く。SecurityContext 上の authority は所持権限を `PERM_` 接頭辞で発行したもので、ロール名は authority に現れない（Spring Security の `ROLE_CAST` / `ROLE_MEMBER` は本人種別の標識であり別物）。
_Avoid_: 能力束・Capability（旧称）、ロールへの店舗の紐付け

**店舗コンテキスト（Store Context）**:
フロントエンドの「現在店舗・授権店舗・店舗切替・店舗リンク生成・ログイン後着地の授権店舗解決」を一手に担う seam（`entities/user` の StoreContextProvider / useStoreContext）。provider は platform / store 両コンソールの layout に搭載され、`me()` + `stores()` は provider で 1 回のみ取得する。店舗パス組立の知識は `shared/lib/store-route`（storePath / storeEntryPath / resolveStoreHref / replaceStoreIdInPath）へ集約し、各所での裸テンプレート字面を禁じる。店舗コンソールへの着地は入口ルート `/store/entry`（`_pages/store-entry`）一箇所に集約する。入口は UI を持たず、授権店舗（前回選択 cookie を優先、無ければ先頭）とメニュー由来の遷移先を解決して差し替え遷移する。着地先をメニューから引くのは、権限を絞ったロールでも必ず自分が到達できる画面に着かせるためで、固定の着地先は権限次第で拒否される。
_Avoid_: 各コンポーネントでの me()/stores() 個別取得、店舗パスの手組み

### 店舗運営

**Cast**:
店舗に在籍する接客担当者。HRM の管理対象。**在籍状態**（在籍中 = ACTIVE / 在籍停止 = INACTIVE）を持ち、指名候補になれるのは在籍中のキャストだけ。
_Avoid_: 在籍状態を「有効／無効」と呼ぶこと（アカウントの停止＝AuthSession のアカウント単位失効と紛らわしい）

**Customer（顧客）**:
店舗の顧客。CRM の管理対象。単一の店舗に帰属する。電話番号は店舗台帳内の検索の**手がかり**であって同定の証明ではない — 受注録入の照合キーとしては使うが、複数一致時は自動照合せず（顧客未設定で成立させる）、会員への帰属根拠には一切しない（ADR 0008 / 0009）。照合キーは第一電話番号のみで、第二番号は連絡先情報に留まる。
_Avoid_: 電話番号を一意キーや本人証明として扱うこと

**顧客統合（Customer Merge）**:
同一店舗内の重複した Customer 行を一つへまとめる操作。存続行へ受注と関連（区間史ごと）を付け替え、被統合行は削除せず**墓標**（統合先参照を持ち、電話照合・一覧・新規参照から除外）にする。旧 ID の解決は常に一跳で統合先へ届く — 連鎖統合時は既存の墓標も新しい統合先へ付け替える。重複候補の提示は同店電話一致等の手がかりでよいが、**統合の実行は必ず人手の確認を経る**。両行が別会員の ACTIVE 関連を持つ場合は拒否する（先に解除してから）。フィールド値の自動合併と統合の取消（undo）は無い — 統合履歴（誰が・いつ・何を移したか。機微情報）が手動修復の根拠になる。ポイント台帳・受注帰属記録には一切波及しない（ADR 0010）。
_Avoid_: 破壊的統合（行の削除）、フィールド値の静黙な自動合併、機械による自動統合

**Member（会員）**:
プラットフォーム層の会員身分。Customer（店舗 CRM 台帳・店舗層）とは別概念であり、Member は複数店舗の Customer 台帳と紐づき得る。非会員の顧客は存在し続ける（Customer だけがあり Member がない状態）。集約の実装は後続チケット（本項は用語の予約）。
_Avoid_: Member と Customer の混用、「会員」を店舗台帳の意味で使うこと

**関連（Customer–Member Link）**:
会員（Member）と店舗顧客台帳の行（Customer）の紐づけ。1 行が 1 区間（ACTIVE → RELEASED）を表す追加型の記録で、**成立根拠**（MEMBER_CODE＝会員コード提示 / MEMBER_REQUEST＝会員申請の確定時自動整備 / MIGRATION＝旧システム移行由来）を事実として持つ。根拠は行ごとに不変で挙動を分けない（MIGRATION も全機能）。より強い根拠での再成立は解除→新しい行で表す。解除は**以後**の帰属を止めるだけで、過去に確定した受注帰属記録・ポイントには波及しない（ADR 0009）。店舗ごとに会員 1 人につき高々 1 本、顧客 1 行につき高々 1 本の ACTIVE。
_Avoid_: 電話番号一致を成立根拠にすること、FHIR 式の抽象等級（level1〜4）で根拠を表すこと、解除で過去の履歴を消すこと

**PointLedger（会員ポイント台帳）**:
プラットフォーム層にある会員ポイントの唯一の正本。全増減（付与・利用・取消・失効・手動調整・退会消去）を仕訳（種別・理由・実行主体・元取引・発生店舗・日時）として記帳し、残高は仕訳から再構成する。期限別残高を持ち、利用は期限の早い仕訳から充当する。会員（Member）に帰属して店舗を跨ぐ（A 店獲得→B 店利用）。発生店舗は帰属属性 `originating_store_id` であり StoreScope の隔離軸ではない（ADR 0006）。非会員（未紐づけ顧客）にポイントは存在しない。
_Avoid_: 店舗レベルの残高列（旧 Customer.points — 廃止済み）を復活させること、残高だけを保存して仕訳を省くこと

**Order（注文）**:
顧客の店舗における 1 回の予約／受注記録。Customer、Cast、および接客担当（PlatformUser）に紐づく。店舗への帰属は `store_id`（StoreScopedEntity）のみで表し、店舗名を非正規化して保存しない（表示が必要な場合は Store への JOIN で導出する。ADR 0003）。
**すべての受注は確定で出生する**（ADR 0017）。店舗・HQ が起こす受注は電話口で受けると決めた時点で可否が判断済みであり、会員の申請は別記録の**予約申請（OrderApplication）**が受けて、店舗の確定操作が CONFIRMED の受注を生む。状態機械は CONFIRMED → COMPLETED / CANCELLED のみ。**受付経路 WEB は申請確定由来の受注だけが名乗る**値であり、店舗・HQ の作成経路では拒否される（広告費と効果集計の根拠になる記録が代理入力で偽装されないため）。
受注の「人」への帰属は Customer（店舗台帳行）に着地する。会員の申請による受注も確定時に台帳行と関連が整えられ、完了時の会員解決（ポイント記帳先）は「顧客 → 有効な関連 → 会員」の一本だけを通る（ADR 0008）。申請者の記録（requester）は申請の出所であって帰属ではない。顧客にも会員にも帰属しない受注（**無帰属受注**）は正規の状態として成立し、売上には数えるがポイントは記帳されず、店舗のみが見る。
**取消**は確定した受注を終わらせる唯一の操作で、理由・実行者・時刻を伴い、二度は行えない。理由は「なぜこの受注が消えたか」を後から辿るための人の判断の記録であって、集計のための分類軸ではない。**取消済み・完了済みは終端**であり、内容の書き換えも行の削除も受け付けない — 誤登録も行を消さず取消として残す。完了後に内容を直すことはこれとは別の概念で、権限と訂正の記録を伴う専用の経路が担う（#380。財務ロックを設けないという方針は変えない）。
_Avoid_: Reservation, Booking、申請の出所（requester）を帰属として扱うこと、受注行を削除して無かったことにすること、取消理由を集計の分類軸として使うこと、受注を未確定で生むこと

**予約申請（OrderApplication）**:
会員（将来はゲストも）が店舗へ出す来店希望の独立した付随記録（`t_order_applications`）。受注（Order）の前室であって来店集約ではない（来店集約は Order の一つだけ、の原則は崩れない）。状態は PENDING → CONFIRMED / DECLINED / WITHDRAWN の四値で、終端に入った申請行は以後動かない — 申請原文（希望内容）は確定内容との対照先として不変のまま残る。**確定＝申請内容を予填した受注の作成操作**で、店舗が補完・調整した内容の Order を CONFIRMED で生成し、申請行へ `order_id` を回写する（シフトの申請行回写と同じ背骨）。謝絶は理由必須（取消 ADR 0013 の先例）。**失効は状態に持たず導出する** — 希望日（営業日）を過ぎた PENDING は表示上失効とし、確定・謝絶を拒否する（バッチ無し。本人の取り下げは可能）。会員申請は本人が入力する**店舗へ名乗る名前**を必須項目として運び、確定時の自動整備が起こす台帳行の氏名（電話は空）になる — 店舗が知る名前はこれだけで、プラットフォーム側プロフィール（表示名・メール）は店舗へ渡らない。申請は顧客参照を持たず、顧客の解決は確定の瞬間に「今の関連」だけを見て行う（ADR 0008）。
_Avoid_: 申請を Order の状態（旧 CREATED）で表すこと、確定前の申請内容を書き換える口を設けること、失効を状態やバッチで物化すること、`Request` を集約名に使うこと（HTTP DTO の命名位）

**受注帰属記録（Order Attribution）**:
受注 1 件が会員へ帰属した事実の受注単位の記録（根拠・帰属時刻・帰属時点の会員コードのスナップショットを持ち、会員の削除後も残る）。帰属は**完了の瞬間**（完了時の関連解決 — 根拠 COMPLETION）または**事後申領の瞬間**（伝票トークン — 根拠 RECEIPT_TOKEN）にだけ生まれ、成立後は関連の状態に影響されない**不変の事実**である（ADR 0009）。会員の来店履歴はこの記録から読む（関連成立以前の受注は帰属記録が無いので見えない）。事後申領の証明は、完了時に会員へ帰属しなかった受注（顧客の有無を問わない）へ発行されるワンタイムの伝票トークン（QR）の所持のみで、申領期限は完了から 90 日（訂正による再発行のみ例外で、再発行から 90 日）。事後申領は店舗台帳（Customer 行・行級の関連）に一切波及しない。関連の成立が過去受注の遡及帰属を引き起こすことはない（ADR 0008）。唯一の訂正操作は理由付きの**無効化**で、ポイントへは自動波及しない（台帳側の訂正は帰属記録を宛先に取る別段の手動調整で行う。ADR 0012）。無効化された受注は会員へ帰属しない状態に戻り、トークン再発行による事後申領の対象へ復帰する。生きた伝票は受注 1 件につき高々 1 本で、再発行は前の 1 本を**失効**させる（機構が起こす副作用であり、人手の無効化とは別の操作。発行の応答を取り逃した店舗は再発行し直せる。ADR 0011）。
_Avoid_: 注文番号の入力を所持の証明として受け付けること、行級の関連（リンク）との混用、無効化を取消連動（受注取消）の代用にすること

**指名 / 指名候補（Nomination / Cast Candidate）**:
Order に紐づく 1 名の Cast が**指名**。**指名候補**は「当該店舗に在籍中の Cast」であり、この条件は候補の読み口と書き込み側の検証がひとつの述語として共有する — 候補に出さないだけでは、キャスト ID を直接送る要求を防げない。確定（Order の確定操作）ではさらに当日の確定シフトを要求する。
店舗側の候補の読み口は受注の配下（受注権限）に置く。指名は受注の操作であり、候補の範囲も要る権限も受注側が決めるため。キャスト管理の一覧は別物で、在籍停止も返し、キャスト管理権限を要求する。
_Avoid_: キャスト管理一覧を候補として流用すること、絞り込みを画面側だけで行うこと

**出勤希望（ShiftRequest）**:
キャストが所属店舗を指定して提出する申請（店舗・日付・時間帯・備考）。種別は 2 つ — **新規希望**（NEW: 承認で確定シフトを新規作成）と、確定済みシフトに対する**変更申請**（CHANGE: 承認で対象シフトを更新）。状態は受付済み（PENDING）・承認（APPROVED）・却下（DECLINED）の 3 つ。承認しても希望そのものはシフトへ変化せず、申請の履歴として残る — NEW の承認時は生成したシフトの id を申請行（shift_id）へ回写し、系列（下記 Shift）の背骨とする。所属店舗の判定は「当該店舗に本人の cast 行が存在すること」で、cast の在籍状態は見ない（在籍停止の統制はアカウント層が担う）。これは出勤希望の所属判定に限った話であり、在籍状態を可否判定に使わないという通則ではない — 受注の指名可否（上記「指名 / 指名候補」）は在籍状態を見る。未取消の当日実績が付いたシフトへの CHANGE は受け付けない（提出時拒否・承認時再検・承認可否導出の三面同条件）。目標営業日が終了した申請は承認できない（判定は日付変更時刻。自動失効は無く、却下は無期限に可能）。
_Avoid_: 希望とシフト（Shift）の混用、変更申請を独立した集約として扱うこと

**Shift（シフト）**:
排班の事実の唯一の正本（`t_shifts`）。状態は **TENTATIVE（店舗内の下書き）** と **CONFIRMED（確定）** の 2 値で、CONFIRMED → TENTATIVE の逆行も許す。TENTATIVE の負向不変量は 3 条 — 店外へ公開されない・受注確定の内部検証（hasConfirmedShift）に数えない・変更申請の対象にならない。系列は**双起点一主幹**: キャスト希望起点（申請 → 承認 → シフト。申請行の shift_id 回写が背骨）と店舗直接作成起点（申請行なし）のどちらも正規の起点で、t_shifts の主幹に合流する。店舗の直接編集は「承認後の調整」として維持する。ただし未取消の当日実績が付いたシフトは work_date とキャストを変更できず（実績が物化した事実と食い違う付け替えを許さない — ADR 0014）、実績行（取消済みを含む）が参照する限り削除できない（誤建の組は実績の取消標記とシフトの TENTATIVE 化で中性化する）。
_Avoid_: 出勤希望（ShiftRequest）との混用、TENTATIVE を「未公開の確定シフト」と読むこと

**シフトの公開可否（Shift Publication）**:
「店外（匿名訪問者+会員）へこのシフトを露出してよいか」の行単位フラグ（既定 = 承認時公開可。非公開化が例外操作で、内密の出勤は承認・作成と同一トランザクションで非公開を指定して出生させる — 公開で生まれてから隠すまでの露出窓を作らない）。シフト側の露出関門は **CONFIRMED ∧ 公開可**で、公式サイト出勤表と会員ポータルの指名候補の両方がこの述語で絞る（キャスト在籍などシフト外の既存条件はそのまま重なる — 単独の十分条件ではない）。片面だけ絞ると軸が形骸化し、開放登録の会員経由の列挙経路になる。同じ理由で、会員経由の指名の**書き込み検証**も候補の読み口とこの述語を共有する — 候補から隠すだけでは cast_id の直送を防げない（上記「指名 / 指名候補」の共有述語規則）。承認とは独立の軸であり、変更申請の承認でシフトが更新されても公開可否は保持される。**負向不変量**: 受注確定の内部検証（hasConfirmedShift＝店舗の確定操作）と店舗側管理画面は公開可否を見ない — 公開可否は店外露出のフィルタであって状態機械の一部ではない（ADR 0015）。切替は SHIFT_MANAGE の日常業務で、専用の留痕は持たない。
_Avoid_: 公開可否を承認状態と混同すること、店舗内の可視制御に流用すること

**当日実績（Attendance）**:
キャストが実際に出勤した事実の一等記録。シフト（予定）とは別集約で、キャスト・店舗・営業日・実際の開始/終了時刻（暦日付き — 事後補記でも帰属営業日が一意に決まる）を持ち、確定シフトへの可空参照（shift_id。1 シフトにつき**未取消の**実績は最大 1 行）を張る（ADR 0014）。予定なしの**飛び込み出勤**は shift_id = NULL の一等記録。行の存在そのものが出勤確認であり、確認フラグ・状態機械は持たない — 欠勤は「その営業日に確定シフトがあり、未取消の実績が無いこと」から（キャスト・店舗・営業日）粒度で導出する（判定は営業日の終了とシフトの予定終了の両方を待つ。未来・進行中は欠勤に数えない）。実績は受注確定の根拠にならない（飛び込みで指名を受けるには従来通り店舗が確定シフトを直接作成する）。記録は SHIFT_MANAGE 保持者（店舗側）が行い、キャスト本人の打刻は無く、事後補記の時限も設けない。**訂正**は就地更新と同時に訂正履歴（編集前の行スナップショット・訂正者・時刻）を残す。実績は物理削除しない（労基法 109 条: 保存 5 年・当分の間 3 年）— 誤建行は取消標記とし、導出・照会から除外する。待機場所は実績行の可空自由記述列。
_Avoid_: 実績をシフトの状態遷移（scheduled → worked）で表すこと、実績行の物理削除

**営業日（Business Date）**:
暦日から独立した帰属単位。プラットフォーム統一の日付変更時刻（SystemConfig）で区切り、帰属は分割しない（全量帰属。25 時閉店の店の深夜勤務は前営業日に属す）。実績行に business_date として物化する。帰属の決め方は二本で、優先順がある — **シフトに紐づく実績はシフトの work_date を継承**（予実は常に同一営業日。実際開始が日付変更時刻を跨いでも変わらない）、**飛び込み（shift_id なし）は実際開始時刻の属する営業日**へシステムが自動判定する（誤りは訂正で直す）。「営業日が終了した」の判定（期限切れ申請の承認拒否・欠勤導出など）もこの日付変更時刻で行い、暦日零時を使わない。日付変更時刻の変更は自由で、将来の判定にのみ作用する（不遡及 — 物化済みの営業日は書き換えない）。
_Avoid_: 暦日 0 時での帰属・期限判定、店舗別の日付変更時刻（実需が確認されるまで）

**StoreProfile（店舗サイト設定）**:
店舗サイトのブランドと外観の設定（テンプレート、logo、banner、SNS リンク等）。
_Avoid_: TenantConfig（旧名。SystemConfig と紛らわしい）

### プラットフォーム設定

**SystemConfig（システム設定）**:
プラットフォームレベルのキーバリュー設定（SMTP、メンテナンスモード等）。SYSTEM_CONFIG_MANAGE 権限の保持者のみが管理できる。
_Avoid_: Config（裸使用。StoreProfile と混同しやすい）

**Menu（メニュー）**:
管理画面のナビゲーションメニューツリー。Central と Store の 2 スコープで同一概念を共有する。コード上は単一の Menu 集約（`t_menus`）へ統合済み（2026-07-18、#404 決定 2 / #409。CentralMenu / StoreMenu は統合前の歴史名）。
_Avoid_: TenantMenu（旧名）

## Open questions（未解決の論点）

- **Customer.rank / classification**：いずれも自由テキストで、等級／区分の正式な値体系は未定義（DB デフォルトは rank='SILVER'）。UI はテキスト入力で暫定対応し、業務側で値集合が確定次第 enum + ドロップダウンに収束させる。
