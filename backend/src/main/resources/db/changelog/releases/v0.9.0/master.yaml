# v0.9.0 = 会員ポータルからの予約申請（受注の申請属性・人数・受付経路）を追加する。
# 構成は v0.1.0 〜 v0.4.0 と同じ platform / store / seed の 3 層のうち store のみ。
databaseChangeLog:
  - include:
      file: store/01-order-member-request.yaml
      relativeToChangelogFile: true
